# Tiddis Tapis — خط أساس التدقيق

## النطاق
تم تدقيق النسخة المحلية المنفصلة من المشروع دون تعديل المستودع أو الملفات الأصلية. النطاق الحالي هو `index.html` و`product.html` و`bahi2005.html` وملفات `js/` و`css/style.css`.

## الهوية الحالية التي يجب الحفاظ عليها
- واجهة إنجليزية باتجاه LTR.
- هوية تحريرية هادئة وفاخرة: خلفية فاتحة دافئة، كحلي/عنابي داكن، خطوط serif للعناوين وSpace Mono للعناصر الوظيفية.
- تخطيط المتجر الحالي يعتمد على sidebar في سطح المكتب وmobile header في الهاتف.
- لا توجد نية لإعادة تصميم كاملة أو إدخال مظهر نيون/داكن؛ Liquid Glass يجب أن يكون طبقة خفيفة في عناصر محددة فقط.

## ما هو موجود فعليًا
### المتجر والبيانات
- المنتجات والفئات والإعدادات والطلبات في Firestore.
- `store.js` يستخدم AppState وفهارس داخلية للتصفية والبحث.
- المنتجات تُقرأ عبر `onSnapshot` بترتيب `createdAt` تنازليًا.
- الفلاتر الحالية تعتمد على أسماء الفئات النصية في `category` و`overviewCategory`.
- المنتجات تدعم `imageUrl` و`additionalImages` و`variants`، والمتغير يدعم `size` و`color` و`price` و`image`.
- نموذج الطلب ينشئ مستندًا في `orders` مع معلومات المنتج والعميل والتوصيل والحالة.
- Google Sheets تكامل اختياري من خلال URL محفوظ داخل `settings/storeSettings`.

### صفحة المنتج
- `product.html` يحتوي على حاوية فارغة تُملأ من `loadProductDetail()` في `store.js`.
- `renderProductDetail()` يعرض الصور عموديًا بالتتابع، ثم صندوق معلومات بسيط.
- تغيير المتغير يحدّث السعر والمقاس واللون ويستبدل أول صورة فقط.
- لا يوجد معرض رئيسي/صور مصغرة/اختيار صورة مستقل.
- توجد أزرار ORDER وPDF، ونموذج الطلب المشترك.
- يوجد timeout مدته 10 ثوانٍ ورسالة Retry، وهو سلوك يجب الحفاظ عليه وتحسين نصوصه فقط.

### لوحة الإدارة bahi2005
- تسجيل دخول Firebase بسيط يعتمد على وجود مستخدم مصادق عليه فقط.
- أقسام فعلية: Dashboard، Categories، Overview Categories، Products، Delivery، Orders، Settings.
- إدارة فئات وفئات فرعية مع سحب وإفلات وحفظ ترتيب.
- إدارة المنتجات مع إضافة/تعديل/حذف، صور إضافية، اختيار صورة PDF، متغيرات، تخصيص مقاس.
- إدارة أسعار الولايات، بما في ذلك تطبيق سعر جماعي/توصيل مجاني/إعادة ضبط.
- جدول الطلبات مع تحديث الحالة والحذف.
- إعدادات Google Sheets، مزود الصور وAPI key، الألوان، النص، الشعار، وروابط التواصل.
- توجد Dashboard وإحصائيات أساسية وآخر الطلبات.

## المشكلات المؤكدة
1. قواعد Firestore الحالية تمنح أي مستخدم مسجل الدخول صلاحية الكتابة الكاملة على المنتجات والفئات والإعدادات والطلبات؛ لا توجد أدوار Admin/Editor/Marketing ولا تحقق مخطط للحقول.
2. `imageApiKey` محفوظ في مستند إعدادات عام تقرؤه الواجهة العامة، وهذا سر يجب عدم عرضه للمتصفح العام أو تخزينه في وثيقة عامة.
3. `googleSheetsUrl` داخل إعدادات يقرأها المتجر العام؛ يجب اعتباره إعدادًا خاصًا أو نقله إلى مسار خادمي/محمي، مع إبقاء الطلب في Firebase كمرجع أساسي.
4. أسماء الفئات والفئات الفرعية مخزنة داخل المنتج كنصوص؛ إعادة التسمية تنفذ تحديثات جماعية، وقد تترك بيانات غير متزامنة إذا فشل جزء من العملية.
5. القيم المدخلة من الإدارة والزائر تُركب في HTML في مواضع عديدة؛ يلزم escape/validation قبل العرض والحفظ لتقليل XSS والبيانات غير الصالحة.
6. `alert` و`prompt` و`confirm` مستخدمة بكثافة في الإدارة وPDF؛ هذا يضعف تجربة الاستخدام ويصعب الوصولية.
7. حالات الطلب الحالية ثلاث فقط: `en attente` و`en cours` و`livrée`، ولا توجد حالات موحدة أو سجل تغيير أو وقت آخر تحديث.
8. صفحة المنتج الحالية لا تحقق قالب المعرض المطلوب: الصورة الرئيسية، thumbnails جانبية، صندوق مواصفات واضح، وزرا شراء/طلب أو إضافة للسلة.
9. روابط الصور الخارجية لا تُدار عبر طبقة موحدة للتحقق من الفشل أو lazy loading أو fallback.
10. HTML يحتوي على inline styles كثيرة، ما يجعل إضافة طبقة Liquid Glass أو صيانة التصميم أصعب؛ يمكن إضافة classes دون إعادة كتابة الهوية كاملة.
11. التحقق الأمني في العميل وحده غير كافٍ؛ قواعد Firestore يجب أن تكون مصدر الإنفاذ الحقيقي، مع claims/roles أو وثيقة أعضاء محمية.
12. لا يوجد مسار ترحيل صريح للبيانات القديمة؛ أي تغيير من أسماء إلى IDs أو من إعدادات عامة إلى خاصة يجب أن يكون backward-compatible وقابلًا للعكس.

## قرارات نطاق التنفيذ
- لا تغيير في الشعار أو الألوان الجوهرية أو الخطوط أو هيكل المتجر العام.
- Liquid Glass محدود إلى صندوق المعلومات/عناصر المعرض وربما بطاقات الإدارة، باستخدام شفافية منخفضة وحدود خفيفة و`backdrop-filter` مع fallback ثابت.
- لا تُضاف سلة تجارة إلكترونية كاملة إلا إذا كان التدفق الحالي يدعمها؛ الأولوية الحفاظ على ORDER وتطويره إلى CTA واضح. يمكن تجهيز طبقة `add to shortlist/cart` مستقبلية دون تغيير تدفق الطلب الحالي.
- لا تُطبق قواعد Firestore الجديدة على المشروع الحي قبل مراجعة المخطط واختبار مستخدم عام وحساب إدارة.
- لا رفع إلى GitHub أو Cloudflare، والتسليم لاحقًا سيكون للملفات المتغيرة فقط.
