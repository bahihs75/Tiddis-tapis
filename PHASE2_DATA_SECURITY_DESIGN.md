# Tiddis Tapis — عقد البيانات وخطة الحماية

## هدف المرحلة
هذه الوثيقة تصميم مرحلي فقط. لا تُطبّق قواعد Firestore ولا تغيّر بيانات الإنتاج قبل مراجعة المستخدم واختبار حساب الإدارة وحساب الزائر. الهدف هو تقوية البنية دون كسر المنتجات الحالية أو تغيير هوية الموقع.

## تقييم القواعد الحالية
القواعد المرسلة تسمح بالقراءة العامة للمنتجات والفئات والإعدادات، وتسمح بإنشاء الطلبات من الزائر، وتمنع قراءة الطلبات إلا للمستخدم المصادق عليه. هذه بداية صحيحة من ناحية فصل الطلبات عن القراءة العامة، لكنها لا تحقق مبدأ أقل صلاحية لأن الشرط `request.auth != null` يمنح كل مستخدم مسجل الدخول صلاحية الكتابة والحذف على المنتجات والفئات والإعدادات والطلبات.

كما أن `settings/storeSettings` يحتوي حاليًا على `imageApiKey` و`googleSheetsUrl`. وبما أن إعدادات المتجر تُقرأ من الواجهة العامة، فإن حفظ مفتاح API أو رابط تكامل داخلي في نفس الوثيقة يجعل هذه القيم قابلة للوصول من المتصفح. يجب نقل الأسرار إلى بيئة خادمية أو حذفها من الوثيقة العامة، وليس الاكتفاء بإخفائها في واجهة لوحة الإدارة.

## عقد بيانات متوافق مع الوضع الحالي
### المنتجات
يُحافظ الإصدار الأول على الحقول الحالية حتى لا تنكسر القراءة:

| الحقل | الحالة الحالية | الخطة المتوافقة |
|---|---|---|
| `name` | مستخدم في المتجر والإدارة | يبقى مطلوبًا بعد التحقق من طول ونوع القيمة |
| `category` | اسم فئة نصي | يبقى مؤقتًا للتوافق، مع إضافة `categoryId` عند توفر المعرّف |
| `overviewCategory` | اسم فئة نصي | يبقى مؤقتًا للتوافق، مع إضافة `overviewCategoryId` اختياريًا |
| `basePrice` | رقم السعر الأساسي | يتحقق من أنه رقم صحيح غير سالب |
| `imageUrl` | الصورة الرئيسية | تبقى مدخلًا أساسيًا مع fallback إذا فشلت الصورة |
| `additionalImages` | مصفوفة روابط | تُنظّف من الفراغات والتكرار ويُحد أقصى منطقي لعدد الصور |
| `variants` | مصفوفة أحجام/ألوان/أسعار/صور | تُطبّع إلى قيم محددة وتُتحقق من السعر والرابط |
| `pdfImage` | صورة ورقة المواصفات | تبقى اختيارية ويجب أن تكون إحدى الصور المسموحة أو رابطًا صالحًا |
| `customizableSize` | Boolean | يُحفظ كـ Boolean حقيقي وليس نصًا |
| `createdAt` و`updatedAt` | ISO strings في الكود الحالي | يضاف `updatedAt` دائمًا، ويفضل لاحقًا Timestamp موحدًا بعد خطة ترحيل |
| `schemaVersion` | غير موجود | يضاف بقيمة `2` للكتابات الجديدة فقط، مع قراءة افتراضية للإصدار القديم |
| `status` | غير موجود | يؤجل حتى تعريف حالات العرض؛ لا يجوز أن يؤدي غيابه إلى اختفاء المنتجات القديمة |
| `sortOrder` | غير موجود | يضاف لاحقًا للسحب والترتيب، مع fallback إلى `createdAt` |

القاعدة العملية هي أن القراءة تكون عبر دالة تطبيع واحدة: إذا وُجدت الحقول الجديدة تُستخدم، وإذا غابت تُستخرج القيم من `category` و`overviewCategory` وحقول الصور القديمة. أما الكتابة في المرحلة الأولى فتحتفظ بالحقول القديمة وتضيف الحقول الآمنة فقط بدل تنفيذ ترحيل شامل.

### الفئات
الهيكل الحالي يستخدم مستند فئة يحتوي على `name` و`subcategories` كمصفوفة أسماء. لن يُستبدل هذا الهيكل فجأة. في النسخة الآمنة التالية، يُضاف `type` و`sortOrder` و`schemaVersion`، ويُحافظ على المصفوفة القديمة إلى أن تُنفذ هجرة مستقلة إلى مجموعة `subcategories` ذات معرّفات. إعادة تسمية فئة يجب أن تتحقق من عدد المنتجات المتأثرة وتعرض تأكيدًا واضحًا قبل الكتابة الجماعية.

### الإعدادات
يُفصل الإعداد إلى مسارين منطقيين:

| المسار | محتوى مسموح | الوصول المقترح |
|---|---|---|
| `settings/storeSettings` | النص، الشعار، الألوان، جهات التواصل، معلومات عرض عامة | قراءة عامة، كتابة لدور إداري محدد |
| `settings/privateIntegrations` أو Secret Manager | Google Sheets URL، مفاتيح مزودي الصور، أي سر | لا قراءة عامة؛ يفضل خادم/Cloud Function أو إزالة التخزين من Firestore العام |
| `settings/deliveryRates` | أسعار الولايات وحالة التوصيل المجاني | قراءة عامة، كتابة لدور إداري محدد |

في المرحلة الأولى، إذا تعذر إنشاء بيئة خادمية، لا ينبغي حفظ `imageApiKey` في وثيقة عامة. يمكن الإبقاء على وضع Direct Link اليدوي، أو مطالبة المستخدم بإدخال المفتاح في بيئة تشغيل محلية/خادمية منفصلة.

### الطلبات
تُوحّد الحالة الجديدة داخليًا بقيم إنجليزية ثابتة، مع قراءة الحالات القديمة:

| القيمة القياسية | الحالات القديمة المقبولة للقراءة |
|---|---|
| `pending` | `en attente`, `new`, الغياب |
| `processing` | `en cours` |
| `contacted` | لا مقابل قديم؛ إضافة جديدة |
| `completed` | `livrée` |
| `cancelled` | لا مقابل قديم؛ إضافة جديدة |

لا يُحذف الحقل القديم مباشرة. عند تحديث طلب قديم، يمكن كتابة `status: 'pending'` مع `legacyStatus` مؤقتًا إذا احتجنا تتبعًا واضحًا، أو تحويل العرض فقط في الإدارة ثم تنفيذ هجرة منفصلة بعد النسخ الاحتياطي. يجب إضافة `statusUpdatedAt` و`statusUpdatedBy` للحالات الجديدة، ولا يجوز للزائر تعديل الطلب بعد إنشائه.

## نموذج الأدوار المقترح
الأدوار المطلوبة هي `admin` و`editor` و`marketing`، لكن الواجهة وحدها لا تكفي لفرضها. المسار الآمن هو استخدام Firebase custom claims:

| الدور | القراءة | المنتجات/الفئات | الطلبات | الإعدادات الحساسة |
|---|---|---|---|---|
| الزائر | المنتجات والإعدادات العامة | لا شيء | إنشاء فقط | لا شيء |
| Marketing | العامة + إحصائيات مسموحة | محتوى العرض والصور، دون القواعد أو التكاملات الحساسة | قراءة محدودة إن لزم | لا شيء |
| Editor | العامة | إضافة/تعديل المنتجات والفئات، دون حذف جماعي أو قواعد | قراءة وتحديث حالة عند الحاجة | لا مفاتيح |
| Admin | كل ما سبق | كامل الإدارة | قراءة/تحديث/حذف مع سجل | إدارة التكاملات والأدوار |

قبل نشر أي قواعد تعتمد على claims، يجب التأكد من وجود claim فعلي لكل حساب. لا يجوز استخدام وثيقة أدوار قابلة للكتابة من المستخدمين كبديل أمني. إذا لم تتوفر Cloud Functions أو Admin SDK بعد، تبقى القواعد الحالية مؤقتًا مع عدم الإدعاء بأنها طبقت الأدوار، وتُؤجل الكتابة الآمنة إلى خطوة إعداد claims.

## مسودة قواعد مقترحة — لا تُنشر بعد
المبدأ الآمن هو تعريف شروط مثل `isAdmin()` و`hasRole(role)` اعتمادًا على `request.auth.token.role`، مع التحقق من نوع الحقول الأساسية داخل `request.resource.data`. يجب أن تسمح `orders.create` فقط بالحقول المتوقعة والأحجام المعقولة، وأن تمنع تغيير `customerPhone` أو `productId` من تحديث إداري غير مصرح. هذه المسودة تحتاج مطابقة أسماء الـclaims الفعلية قبل وضعها في ملف `firestore.rules`.

مثال الهيكل المنطقي وليس ملف نشر نهائيًا:

```text
products/categories:
  read: public
  create/update: editor or admin
  delete: admin

settings/storeSettings and deliveryRates:
  read: public
  write: admin

settings/privateIntegrations:
  read/write: admin through protected path or server

orders:
  create: public after field/type/length validation
  read/update: admin, or explicitly scoped role if the business approves
  delete: admin only
```

## خطة الترحيل الآمنة
أولًا تُؤخذ نسخة JSON من المنتجات والفئات والإعدادات والطلبات، ويُختبر الاستيراد في مشروع Firebase تجريبي أو نسخة محلية. ثانيًا تُضاف دوال تطبيع لا تغيّر البيانات، ثم تُحدّث لوحة الإدارة لتكتب الحقول الجديدة مع القديمة عند الحاجة. ثالثًا تُراقب القراءة والتصفية وصفحة المنتج. بعد التأكد من عدم وجود منتجات مفقودة، تُجرى هجرة دفعات صغيرة تضيف IDs و`schemaVersion` وحقول الحالات. أخيرًا فقط تُفعّل القواعد الأكثر تشددًا، مع الاحتفاظ بخطة رجوع للقواعد السابقة.

## قرار هذه المرحلة
لا تُعدل ملفات الإنتاج الآن ولا تُنشر القواعد الحالية بصيغتها الجديدة. الخطوة التنفيذية الآمنة التالية، بعد موافقة المستخدم على النطاق، هي إضافة طبقة تطبيع/تحقق مشتركة محليًا، ثم تحديث لوحة الإدارة والطلبات وصفحة المنتج، وبعد اختبارها إعداد ملف قواعد منفصل للاختبار والمراجعة.
